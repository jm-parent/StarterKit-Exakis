<?xml version="1.0" encoding="utf-8" ?>
<popups:MyOwnPopup  xmlns:popups="clr-namespace:StarterKit.Views.Popups" xmlns:xct="http://xamarin.com/schemas/2020/toolkit" 
            xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            x:Class="StarterKit.Views.Popups.MyPopupView"
            x:Name="ThePopup"
            Size="300,300"
            >
    <Frame BackgroundColor="Transparent">
        <Frame BackgroundColor="DarkGray" Margin="150" CornerRadius="8" x:Name="TheContent">
            <StackLayout>
                <Label Text="Hello from popup!" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" TextColor="WhiteSmoke" MaxLines="1"  x:Name="TheLabel" FontSize="1"/>
            <Button Text="Close me" Clicked="Button_Clicked" x:Name="TheButton" FontSize="1" />
            </StackLayout>
        </Frame>
    </Frame>
</popups:MyOwnPopup>